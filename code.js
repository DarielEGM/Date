"use strict";

const fecha = new Date();

console.log(`Month:${fecha.getMonth()+1}  Day:${fecha.getDate()} Year:${fecha.getYear()+1900} 
Hour:${fecha.getHours()} Minutes:${fecha.getMinutes()} Seconds:${fecha.getSeconds()}`); 